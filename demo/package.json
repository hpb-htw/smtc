{
  "name": "zmdc-demo",
  "version": "0.0.1",
  "description": "Demo Zeig mir den Code",
  "type": "module",
  "scripts": {
    "build": "npx vite build; mkdir -p docs/code; cp lib/*.js docs/code ; mkdocs build; cp README.md site/",
    "clean": "rm -rf site; rm -rf docs/code/* ; rm -rf docs/lib/"
  },
  "author": "Hong-Phuc Bui",
  "license": "MIT",
  "dependencies": {
    "@picocss/pico": "2.1.1",
    "prismjs": "1.30.0"
  },
  "devDependencies": {
    "vite": "6.2.5"
  }
}
